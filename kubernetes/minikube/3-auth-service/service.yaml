apiVersion: v1
kind: Service
metadata:
  name: ithust-auth
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: ithust-auth
  ports:
  - name: auth
    port: 4002
    targetPort: 4002
    protocol: TCP