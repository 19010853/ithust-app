# Production build values (CI / hosting inject real secrets; do not commit .env.production with secrets)
# Example only — replace YOUR_DOMAIN and keys.

VITE_NODE_ENV=production
VITE_BASE_ENDPOINT=https://YOUR_DOMAIN.com
VITE_CLIENT_ENDPOINT=https://YOUR_DOMAIN.com
VITE_STRIPE_KEY=pk_live_REPLACE_ME
VITE_ELASTIC_APM_SERVER=https://apm.YOUR_DOMAIN.com
